pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Cloning QuizApp repository'
            }
        }

        stage('Build') {
            steps {
                echo 'Static website - no build required'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment completed'
            }
        }
    }
}
